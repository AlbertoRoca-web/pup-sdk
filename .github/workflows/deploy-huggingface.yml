name: Deploy Alberto to HuggingFace Spaces

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out repository
      uses: actions/checkout@v4
      
    - name: Setup Git
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        
    - name: Clone HuggingFace Space
      env:
        HF_TOKEN: ${{ secrets.HUGGINGPUP }}
      run: |
        if [ -z "$HF_TOKEN" ]; then
          echo "❌ HUGGINGPUP secret not found"
          exit 1
        fi
        
        echo "✅ Found HUGGINGPUP token"
        git clone https://${HF_TOKEN}@huggingface.co/spaces/AlbertoRoca96-web/pup-sdk.git hf-space
        
    - name: Copy files to HuggingFace Space
      run: |
        # Copy all source files to the HuggingFace Space repo
        cp -r app.py Dockerfile requirements.txt README.md hf-space/
        
    - name: Commit and push to HuggingFace
      run: |
        cd hf-space
        git add .
        git commit -m "🐕 Auto-deploy Alberto mobile interface [skip ci]"
        git push
        
        if [ $? -eq 0 ]; then
          echo "🎉 SUCCESS! Alberto deployed to HuggingFace Spaces!"
          echo "🌐 Space URL: https://huggingface.co/spaces/AlbertoRoca96-web/pup-sdk"
          echo "⏱️ Build starting now... wait 2-5 minutes"
          echo "📱 Alberto will be live on mobile soon!"
        else
          echo "❌ Deployment failed!"
          exit 1
        fi