name = "pup-sdk"
main = "src/worker.mjs"
compatibility_date = "2024-12-01"

# If Cloudflare added account_id/project_id for you, keep them
# account_id = "xxxxxxxxxxxxxxxxxxxx"
# workers_dev = true

[vars]
# These *defaults* are only for local/dev; in production you already
# configured OPEN_API_KEY / SYN_API_KEY in the Cloudflare dashboard.
# You can leave them blank or remove this block if you prefer.
OPEN_API_KEY = ""
SYN_API_KEY = ""
